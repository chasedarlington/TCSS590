name: tcss590hw2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - setuptools
  - wheel
  - pip:
      - gymnasium==0.29.1
      - gymnasium-robotics==1.2.4
      - matplotlib==3.8.4
      - mujoco==2.3.7
      - numpy==1.26.4
      - packaging==23.1
#      - pybullet==3.2.5 # pybullet wheel unavailable for amd64 windows on python 3.10 -> Microsoft Visual C++ 14.0 or greater is required. (i don't have admin rights to install this on UW machines)
      - torch==2.3.1
      - torchaudio==2.3.1
      - torchvision==0.18.1

      

      
# gymnasium[mujoco]
